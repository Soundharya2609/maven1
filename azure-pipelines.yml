trigger:
  - main

pool:
  name: Default
  demands:
    - agent.os -equals Windows_NT


steps:
- checkout: self

- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: 'target'
    ArtifactName: 'maven1-artifact'
    publishLocation: 'Container'

